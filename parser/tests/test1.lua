function shellsort(t, befo1re, n1)
    n = n

    k,z,c,v = 0x3F,'sada\n',"sdda\\\"\n", [[
    sdasd asd asd asd asd
    dasdasd \n\"]]
    if not before or before == "<" then return ssup(t, n)
    elseif before == ">" then return ssdown(t, n)
    else return ssgeneral(t, n, before)
    end
    ins.a.d[r.t]
    ina[a]
    return shellsort
end
